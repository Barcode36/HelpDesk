<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.helpdesk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_unicorn_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_unicorn_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ActivityAddAccount"></activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".TechnicianActivity" />
        <activity android:name=".TicketDetail" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TechnicianDetail" />
        <activity android:name=".AdminDetail" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>